{% extends 'back/index.html.twig' %}
{% block title %}TagCours{% endblock %}

{% block bodyback %}
    <h1>TagCours</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tag_cour.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('tag_cours_index') }}">back to list</a>

    <a href="{{ path('tag_cours_edit', {'id': tag_cour.id}) }}">edit</a>

    {{ include('tag_cours/_delete_form.html.twig') }}
{% endblock %}
