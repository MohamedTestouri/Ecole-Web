<form method="post" action="{{ path('users_activer', {'id': user.id}) }}" onsubmit="return confirm('Are you sure you want to activer this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user.id) }}">
    <button class="btn">Activer</button>
</form>
