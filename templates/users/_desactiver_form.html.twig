<form method="post" action="{{ path('users_desactiver', {'id': user.id}) }}" onsubmit="return confirm('Are you sure you want to desactiver this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user.id) }}">
    <button class="btn">Desactiver</button>
</form>
