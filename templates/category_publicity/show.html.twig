{% extends 'back/index.html.twig' %}
{% block title %}CategoryPublicity{% endblock %}

{% block bodyback %}
    <h1>CategoryPublicity</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category_publicity.id }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ category_publicity.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('category_publicity_index') }}">back to list</a>

    <a href="{{ path('category_publicity_edit', {'id': category_publicity.id}) }}">edit</a>

    {{ include('category_publicity/_delete_form.html.twig') }}
{% endblock %}
