<form method="post" action="{{ path('category_publicity_delete', {'id': category_publicity.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ category_publicity.id) }}">
    <button class="btn">Delete</button>
</form>
