<form method="post" action="{{ path('reclamation_delete_client', {'id': reclamation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reclamation.id) }}">
    <div style="text-align: center" >
        <button class="btn btn-dark" class="btn">Supprimer</button>
    </div>
</form>
