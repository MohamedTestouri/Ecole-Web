{% extends 'back/base.html.twig' %}

 {% block main %}

<center>
    <div class="d-flex justify-content-between align-items-center mb-3">
        <h4></h4>
    </div>
     <form style="display: inline;"class="form-inline my-2 my-lg-0"  method="post" action="{{ path("recherche") }}" class="ficon ft-search">
         <div class="col-lg-3 col-md-6 col-12">
         <input class="form-control mr-sm-2" name="search" type="search" placeholder="Rechercher un cours... " aria-label="Search" >
         </div>
     </form>
</center>
    <!-- ======= Courses Section ======= -->

    <section id="courses" class="courses">
        <div class="row">
        {% for tablecour in tablecours %}

            <div class="col-lg-3 col-md-6 col-12">

            <div class="container" data-aos="fade-up">

                <div class="row" data-aos="zoom-in" data-aos-delay="100">


                        <div class="course-item">
                            {% if tablecour.imageC %}
                                <img src="/assets/img/cours/{{ tablecour.imageC }}" class="img-fluid" alt="..." style="width: 100%; height:auto;">
                            {% endif %}
                            <div class="course-content">
                                <div class="d-flex justify-content-between align-items-center mb-3">
                                    <h4>{{ tablecour.Duree }}</h4>
                                </div>

                                <h3><a href="{{ path('show_onecours', {'id':tablecour.id}) }}">{{ tablecour.Titre }}</a></h3>
                                <p>{{ tablecour.Contenu }}</p>

                                <table>
                                    <td>
                                        <button align="center" type="button" class="btn btn-dark" >
                                            <a href="{{ path('reclamation_new',{'titre': tablecour.Titre  }) }}">Réclamer</a>
                                        </button>
                                    </td>
                                </table>
                            </div>

                    </div> <!-- End Course Item-->
                </div>
            </div>
            </div>
        {% endfor %}
    </div>

    </section><!-- End Courses Section -->

<div class="navigation">
    {{ knp_pagination_render(tablecours) }}
</div>



 {% endblock %}


