{% extends 'base.html.twig' %}

{% block title %}Paiement{% endblock %}

{% block body %}
    <h1>Paiement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ paiement.id }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ paiement.prix }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('controller_paiement_index') }}">back to list</a>

    <a href="{{ path('controller_paiement_edit', {'id': paiement.id}) }}">edit</a>

    {{ include('controller_paiement/_delete_form.html.twig') }}
{% endblock %}
