{% extends 'back/index.html.twig' %}

{% block title %}Question{% endblock %}

{% block bodyback %}
    <h1>Question</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ question.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ question.type }}</td>
            </tr>
            <tr>
                <th>Question_a</th>
                <td>{{ question.questionA }}</td>
            </tr>
            <tr>
                <th>Reponse</th>
                <td>{{ question.reponse }}</td>
            </tr>
            <tr>
                <th>Fausse_reponse</th>
                <td>{{ question.fausseReponse }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('question_controller1_index') }}">back to list</a>

    <a href="{{ path('question_controller1_edit', {'id': question.id}) }}">edit</a>

    {{ include('question_controller1/_delete_form.html.twig') }}
{% endblock %}
