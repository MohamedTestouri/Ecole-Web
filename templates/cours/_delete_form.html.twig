<form method="post" action="{{ path('cours_delete', {'id': tablecour.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tablecour.id) }}">
    <div style="text-align: center" >
    <button class="btn btn-dark">Supprimer</button>
    </div>
</form>
